"use strict";

window.PMMG_LESSONS = window.PMMG_LESSONS || {};

// Conteúdo de Processo Penal ainda não foi publicado neste arquivo.
// Mantemos o arquivo válido para não interromper o carregamento dos demais módulos.
Object.assign(window.PMMG_LESSONS, {});
